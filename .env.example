# Local secrets for `vercel dev` — never commit this file
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
CRON_SECRET=
# Optional. If unset, manage UI uses CRON_SECRET.
ADMIN_SECRET=
