{
  "name": "uptime-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Free PWA uptime monitor with Web Push alerts, deployed on Vercel",
  "scripts": {
    "generate-vapid": "npx web-push generate-vapid-keys",
    "dev": "vercel dev"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "engines": {
    "node": ">=18"
  }
}
